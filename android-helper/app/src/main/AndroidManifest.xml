<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools">

    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.ACCESS_NETWORK_STATE" />
    <uses-permission android:name="android.permission.ACCESS_WIFI_STATE" />

    <!-- NSD multicast — some OEMs require this to receive mDNS
         responses on the WiFi NIC; without it the _adb-tls-connect._tcp
         service browse returns nothing on those builds. Harmless on
         OEMs that ignore the flag. -->
    <uses-permission android:name="android.permission.CHANGE_WIFI_MULTICAST_STATE" />

    <!-- Programmatic enable of wireless debugging. Signature-only; must
         be granted once via ADB over USB:
           adb shell pm grant com.folia.bridgehelper \
             android.permission.WRITE_SECURE_SETTINGS
         If not granted, the helper falls back to opening the wireless
         debugging settings panel and asking the operator to flip the
         toggle manually. -->
    <uses-permission android:name="android.permission.WRITE_SECURE_SETTINGS"
        tools:ignore="ProtectedPermissions" />

    <application
        android:allowBackup="false"
        android:icon="@mipmap/ic_launcher"
        android:roundIcon="@mipmap/ic_launcher"
        android:label="Folia Bridge Helper"
        android:supportsRtl="true"
        android:theme="@android:style/Theme.Material.Light.NoActionBar">
        <activity
            android:name=".MainActivity"
            android:exported="true">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>
</manifest>
