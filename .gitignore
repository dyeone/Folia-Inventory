# Logs
logs
*.log
npm-debug.log*
yarn-debug.log*
yarn-error.log*
pnpm-debug.log*
lerna-debug.log*

node_modules
dist
dist-ssr
*.local

# Editor directories and files
.vscode/*
!.vscode/extensions.json
.idea
.DS_Store
*.suo
*.ntvs*
*.njsproj
*.sln
*.sw?
plant-inventory/

# Claude Code per-project config (local only)
.claude/

# Bridge secrets — never commit. The BRIDGE_TOKEN in bridge/.env
# authenticates as the user and can drive the Palmstreet phone if it
# leaks. Rotate via /api/bridge?action=generate-token if exposed.
bridge/.env
bridge/.wireless-target

# Android build artifacts (per-machine SDK path + Gradle outputs)
android-helper/local.properties
android-helper/.gradle/
android-helper/build/
android-helper/app/build/
.gstack/
