{
  "name": "folia-bridge",
  "version": "0.2.0",
  "private": true,
  "type": "module",
  "description": "Local ADB poller that drives the Palmstreet Android app from Folia. Polls Vercel for jobs (no inbound port, no tunnel).",
  "scripts": {
    "start": "node index.js"
  }
}
